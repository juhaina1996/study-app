import React from "react";
import styles from "./TakeTest.module.scss"; // Create a separate module for styling

function TakeTest() {
  return (
    <div className={styles.takeTestPage}>
      <h1>Take the Test</h1>
      {/* Add content for the test here */}
    </div>
  );
}

export default TakeTest;
